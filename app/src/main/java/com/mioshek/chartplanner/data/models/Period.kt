package com.mioshek.chartplanner.data.models

import java.time.Month

enum class PeriodType{
    YEAR(),
    MONTH()
}

class Period constructor(type: PeriodType, numberOfDays: Int, ){
    fun test(){

    }
}